export { Message, default as MessageComponent } from "./Message";
export { ChatInput, default as ChatInputComponent } from "./ChatInput";
export { ChatWindow, default as ChatWindowComponent } from "./ChatWindow";

// Re-export types for convenience (type-only)
export type { Message as ChatMessage } from "../../types/agent";

